# In dev, Vite proxies /api to the backend (see vite.config.js), so this can
# stay unset locally. Set it explicitly for production builds where the
# frontend and backend are deployed on different origins/domains.
# VITE_API_BASE_URL=https://your-backend-domain.com/api
